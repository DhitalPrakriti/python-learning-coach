# === Core Dependencies ===
# Lower bound, not floating: agents/base_agent.py relies on the 2.x
# AutomaticFunctionCallingConfig, which does not exist in 1.x.
google-genai>=2.0.0,<3.0.0
google-cloud-firestore>=2.16.0

# === Web Framework ===
flask>=3.0.0
gunicorn>=21.0.0
werkzeug>=3.0.0

# === Configuration ===
python-dotenv>=1.0.0

# === Data Validation ===
pydantic>=2.0.0

# === Utilities ===
requests>=2.31.0
pyyaml>=6.0
click>=8.1.0

# === Security ===
cryptography>=41.0.0

# === Development ===
# Dev/test tooling lives in requirements-dev.txt so it stays out of the image.
